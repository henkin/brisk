using System;

namespace Brisk.Tests
{
    /// <summary>
    /// This is where we would have the unit tests for the Repository LINQ logic
    /// We would
    /// 1) Abstract the underlying storage into a dependency (eg IDatabase), implement LiteDbDatabase : IDatabase
    /// 2) Inject it into Repository 
    /// 3) Write unit tests for Repository Linq logic
    /// </summary>
    public class RepositoryTests
    {
    }
}